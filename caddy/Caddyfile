# =============================================================================
# Caddyfile - メイン設定
# =============================================================================
# 各アプリの設定は sites/*.caddy に配置し、import で読み込む。
# アプリをデプロイするたびに sites/ に設定ファイルが追加される。
# =============================================================================

# グローバル設定
{
	# メールアドレス（Let's Encrypt の通知用）
	# email admin@example.com
}

# 各アプリの設定を読み込み
import /etc/caddy/sites/*.caddy
